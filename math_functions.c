
/*Pentru calculul Factorial*/
unsigned int Factorial(unsigned int in_factor)
{
	unsigned char index;
	unsigned int retValue = 1;
	for(index = 2; index < in_fator; index++)
	{
		retValue *= index;
	}
	return retValue;
}

/*Pentru calculul X la puterea n*/
unsigned int X_power_of_N(unsigned int x, unsigned char n)
{
	unsigned int retValue ;
	unsigned char index;
	retValue = n
	for(index = 0; index < n; index++)
	{
		retValue *= x; 
	}
	return retValue;
}

